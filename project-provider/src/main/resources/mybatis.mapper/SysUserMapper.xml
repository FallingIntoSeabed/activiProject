<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jfs.stud.mapper.SysUserMapper">
    <resultMap id="BaseResultMap" type="jfs.stud.entity.SysUser">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="sex" property="sex" />
        <result column="phone" property="phone" />
    </resultMap>
    
    <sql id="columnsql">
        select id,name,sex,phone from sys_user
    </sql>


    <select id="selectSysUserById" parameterType="String" resultMap="BaseResultMap">
        <include refid="columnsql" />
        where id = #{id}
   </select>

    <select id="selectAll" resultMap="BaseResultMap">
        <include refid="columnsql" />
    </select>
</mapper>